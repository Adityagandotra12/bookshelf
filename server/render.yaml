# Canonical Render Blueprint lives at the repository root: ../render.yaml
# Render only auto-discovers render.yaml at the repo root.
#
# Backend Web Service settings:
#   Root Directory: server
#   Build Command:  npm install && npm run build
#   Start Command:  npm start
#   Health check:   /health
