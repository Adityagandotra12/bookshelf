# Frontend (Vite). Used when deploying to Vercel.
# Local dev: leave unset to use /api proxy to backend.
# Production: set to your Render backend URL (no trailing slash).
# VITE_API_URL=https://your-app-name.onrender.com
